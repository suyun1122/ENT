Build a Surgical Video Insight Application that analyzes surgical procedure videos using Twelve Labs API and YOLO-based surgical tool detection.

IMPORTANT: The surgical tool detection model has ALREADY BEEN TRAINED. Use the existing trained model - DO NOT train a new model.

PROJECT STRUCTURE (Create from scratch):
1. Frontend: Next.js 14+ app with App Router
   - Directory structure:
     * app/ - Next.js App Router directory
       - page.js - Main dashboard page (/)
       - layout.js - Root layout with NavBar
       - upload/ - Video upload page
         - page.js
       - procedure/[id]/ - Procedure analysis page
         - page.js
       - reports/ - Reports library page
         - page.js
       - api/ - API routes (see code examples below)
         - analysis/route.js
         - analysis/[videoId]/route.js
         - video/route.js
         - search/route.js
         - timeline/route.js
       - components/ - React components
         - VideoPlayer.js
         - ToolDetectionOverlay.js
         - ToolTimeline.js
         - ChapterTimeline.js
         - EventTimeline.js
         - ReportViewer.js
         - VideoChat.js
         - UploadVideo.js
         - NavBar.js
   - Use Tailwind CSS for styling
   - Medical/clinical theme (blues, whites, greens)

2. Backend: FastAPI worker for video processing
   - main.py - FastAPI application
   - cv_pipeline.py - Tool detection integration
   - Process videos, run YOLO inference, upload to Twelve Labs

3. CV Model: Use the PRE-TRAINED YOLO11m model (best.pt) - training is already complete
   - Model file: cv_model/training_scripts/best.pt
   - DO NOT train - use existing model

PRE-TRAINED MODEL DETAILS:
- Model file location: cv_model/training_scripts/best.pt
- Model type: YOLO11m
- Training status: COMPLETED (100 epochs, mAP50: 0.871)
- Reference inference code: See inference code example below
- DO NOT retrain the model - use the existing best.pt file directly

CORE FEATURES:

1. VIDEO UPLOAD & PROCESSING
- Upload surgical videos (MP4, MOV)
- Show real-time processing progress
- Chunk long videos for efficient processing
- Support both file upload and RTSP streams

2. SURGICAL TOOL DETECTION (USE PRE-TRAINED MODEL)
The model training is ALREADY COMPLETE. Use the existing trained model file.

Pre-trained Model:
- Model file: cv_model/training_scripts/best.pt (ALREADY EXISTS - DO NOT TRAIN)
- Model performance: mAP50: 0.871, Precision: 0.889, Recall: 0.825
- Training completed: 100 epochs on Cholec80 dataset

Detect 7 surgical tools (model already trained for these):
- Bipolar, Clipper, Grasper, Hook, Irrigator, Scissors, Specimen Bag

Implementation (use existing model):
- Load the pre-trained model: YOLO("cv_model/training_scripts/best.pt")
- Use Ultralytics YOLO library
- Detection parameters: conf=0.4, iou=0.45, imgsz=640
- Draw bounding boxes on video frames with tool labels and confidence scores
- Generate annotated video output
- Extract detection data: tool names, confidence scores, bounding boxes, timestamps

Reference Inference Code (use this pattern):
```python
"""
Inference script for surgical tool detection model.
"""
import argparse
from pathlib import Path
from ultralytics import YOLO
import cv2

def main():
    parser = argparse.ArgumentParser(description='Run inference on surgical tool detection model')
    parser.add_argument('--model', type=str, required=True,
                        help='Path to trained model weights (.pt file)')
    parser.add_argument('--source', type=str, required=True,
                        help='Path to image, video, or directory, or RTSP stream URL')
    parser.add_argument('--conf', type=float, default=0.25,
                        help='Confidence threshold (default: 0.25)')
    parser.add_argument('--iou', type=float, default=0.45,
                        help='IOU threshold for NMS (default: 0.45)')
    parser.add_argument('--imgsz', type=int, default=640,
                        help='Image size for inference (default: 640)')
    parser.add_argument('--save', action='store_true',
                        help='Save annotated results')

    args = parser.parse_args()

    # Load model
    model = YOLO(args.model)

    # Run inference
    results = model.predict(
        source=args.source,
        conf=args.conf,
        iou=args.iou,
        imgsz=args.imgsz,
        save=args.save
    )

    # Process results: extract tool names, confidence, bboxes, timestamps
    for i, result in enumerate(results):
        if hasattr(result, 'boxes') and result.boxes is not None:
            num_detections = len(result.boxes)
            for box in result.boxes:
                cls_id = int(box.cls[0])
                conf = float(box.conf[0])
                cls_name = model.names[cls_id]
                # Extract bounding box coordinates
                x1, y1, x2, y2 = box.xyxy[0]
                # Process detection data
```

3. TWELVE LABS INTEGRATION (USE EXISTING API ENDPOINTS)
The Twelve Labs integration is ALREADY IMPLEMENTED. Use these exact API endpoint patterns:

EXISTING API ENDPOINT CODE - Copy these patterns:

A) Analysis Endpoint (app/api/analysis/route.js):
```javascript
import { TwelveLabs, TwelvelabsApi } from 'twelvelabs-js';

const twelvelabs_client = new TwelveLabs({apiKey: process.env.TWELVELABS_API_KEY});

export async function POST(request) {
    const { userQuery, videoId } = await request.json();

    try {
        const response = await twelvelabs_client.analyze({
            videoId: videoId,
            prompt: userQuery,
            temperature: 0.2
        })

        return new Response(JSON.stringify(response), { status: 200 });

    } catch (error) {
        console.error("Error during analysis", error);

        if (error.message && error.message.includes('video_not_ready')) {
            return new Response(JSON.stringify({
                code: 'video_not_ready',
                message: 'The video is still being indexed. Please try again once the indexing process is complete.'
            }), { status: 202 });
        }

        if (error.body && error.body.code === 'parameter_invalid' &&
            error.body.message && error.body.message.includes('video_id parameter is invalid')) {
            return new Response(JSON.stringify({
                code: 'video_not_uploaded',
                message: 'The video is still being uploaded and processed. Please wait for the upload to complete.'
            }), { status: 202 });
        }

        return new Response(JSON.stringify({
            code: 'analysis_error',
            error: 'Error during analysis'
        }), { status: 500 });
    }
}
```

B) Video Upload Endpoint (app/api/video/route.js):
```javascript
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';
import { getSignedUrl } from '@aws-sdk/s3-request-presigner';
import { TwelveLabs, TwelvelabsApi } from 'twelvelabs-js';

const twelvelabs_client = new TwelveLabs({apiKey: process.env.TWELVELABS_API_KEY});

const s3_client = new S3Client({
    region: process.env.AWS_REGION,
    credentials: {
        accessKeyId: process.env.AWS_ACCESS_KEY_ID,
        secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY
    }
})

export async function GET(request) {
    const videoPager = await twelvelabs_client.indexes.videos.list(process.env.NEXT_PUBLIC_TWELVELABS_MARENGO_INDEX_ID);
    const videoPagerPegasus = await twelvelabs_client.indexes.videos.list(process.env.NEXT_PUBLIC_TWELVELABS_PEGASUS_INDEX_ID);
    const videoList = {}

    for await (const video of videoPager.data) {
        const fileName = video.systemMetadata.filename
        videoList[fileName] = {
            ...video,
            ...video.systemMetadata
        }
    }

    for await (const video of videoPagerPegasus.data) {
        const fileName = video.systemMetadata.filename
        videoList[fileName] = {
            ...videoList[fileName],
            'pegasusId': video.id
        }
    }

    return new Response(JSON.stringify(videoList), { status: 200 });
}

export async function POST(request) {
    try {
        const { fileName, fileType } = await request.json();
        const fileKey = `uploads/${Date.now()}_${fileName}`;

        const command = new PutObjectCommand({
            Bucket: process.env.AWS_SOURCE_S3_BUCKET,
            Key: fileKey,
            ContentType: fileType,
        })

        const signedUrl = await getSignedUrl(s3_client, command, { expiresIn: 3600 })
        return new Response(JSON.stringify({ uploadUrl: signedUrl, key: fileKey }), { status: 200 });

    } catch (error) {
        console.error("Error generating signed URL", error);
        return new Response(JSON.stringify({ error: 'Error generating signed URL' }), { status: 500 });
    }
}
```

C) Search Endpoint (app/api/search/route.js):
```javascript
import { TwelveLabs } from "twelvelabs-js";

const twelvelabs_client = new TwelveLabs({apiKey: process.env.TWELVELABS_API_KEY});

export async function POST(request) {
    const { query, groupBy, threshold } = await request.json();

    const response = await twelvelabs_client.search.query({
        indexId: process.env.NEXT_PUBLIC_TWELVELABS_MARENGO_INDEX_ID,
        searchOptions: ['visual', 'audio'],
        queryText: query,
        groupBy: groupBy,
        threshold: threshold,
    })

    return new Response(JSON.stringify(response), { status: 200 });
}
```

D) Timeline Endpoint (app/api/timeline/route.js):
```javascript
import { TwelveLabs } from "twelvelabs-js";

const twelvelabs_client = new TwelveLabs({apiKey: process.env.TWELVELABS_API_KEY});

export async function POST(request) {
    const { videoId, prompt, type } = await request.json();

    try {
        const res = await twelvelabs_client.summarize({
            videoId: videoId,
            type: type,
            prompt: prompt,
        })

        return new Response(JSON.stringify(res), { status: 200 });

    } catch (error) {
        console.error("Error during timeline generation", error);

        if (error.message && error.message.includes('video_not_ready')) {
            return new Response(JSON.stringify({
                code: 'video_not_ready',
                message: 'The video is still being indexed. Please try again once the indexing process is complete.'
            }), { status: 202 });
        }

        return new Response(JSON.stringify({
            code: 'timeline_error',
            error: 'Error generating timeline'
        }), { status: 500 });
    }
}
```

E) Video Gist Endpoint (app/api/analysis/[videoId]/route.js):
```javascript
import { TwelveLabs, TwelvelabsApi } from 'twelvelabs-js';

const twelvelabs_client = new TwelveLabs({apiKey: process.env.TWELVELABS_API_KEY});

export async function GET(request, { params }) {
    const { videoId } = await params;
    try {
        const gist = await twelvelabs_client.gist({
            videoId: videoId,
            types: ['title', 'topic', 'hashtag']
        })
        return new Response(JSON.stringify(gist), { status: 200 });
    } catch (error) {
        console.error("Error fetching gist", error);

        if (error.message && error.message.includes('video_not_ready')) {
            return new Response(JSON.stringify({
                code: 'video_not_ready',
                message: 'The video is still being indexed. Please try again once the indexing process is complete.'
            }), { status: 202 });
        }

        return new Response(JSON.stringify({
            code: 'gist_error',
            error: 'Error fetching gist'
        }), { status: 500 });
    }
}
```

How to use /api/analysis for different purposes:
- Chat/Q&A: POST /api/analysis with { userQuery: "user's question", videoId: "..." }
- Tool Detection Results: POST /api/analysis with { userQuery: "Analyze this surgical video and provide a detailed breakdown of all surgical tools detected, including: tool names, frequency of use, timestamps when each tool appears, and confidence scores. Format the response as JSON with tool statistics.", videoId: "..." }
- Report Generation: POST /api/analysis with { userQuery: "Generate a comprehensive surgical procedure report for this video including: procedure summary, tool usage statistics, key moments, timeline of tool transitions, and any notable observations. Format as a structured report.", videoId: "..." }

4. AUTOMATED REPORT GENERATION
Generate comprehensive PDF/JSON reports including:
- Procedure metadata (date, duration, procedure type)
- Tool usage statistics:
  * Total detections per tool
  * Average confidence per tool
  * Tool usage timeline
  * Most/least frequently used tools
- Key moments and highlights
- Visual charts and graphs (tool frequency, timeline visualization)
- Export options: PDF, JSON, CSV

5. UI COMPONENTS AND PAGES (Create these from scratch):

Main Dashboard Page (/ - app/page.js):
- Display grid of surgical procedure cards
- Each card shows: procedure name, date, thumbnail, tool detection summary
- Search and filter functionality
- "Upload New Procedure" button
- Medical/clinical design theme

Video Upload Page (/upload - app/upload/page.js):
- Drag-and-drop video upload interface
- File selection (MP4, MOV)
- Upload progress indicator
- Video preview before upload
- Form fields: procedure name, date, type

Procedure Analysis Page (/procedure/[id] - app/procedure/[id]/page.js):
- Video player component with controls
- Tool detection overlay on video (bounding boxes with labels)
- Interactive timeline showing tool detections (color-coded by tool type)
- Chapter timeline component (shows video chapters from Twelve Labs)
- Event timeline component (shows key moments)
- Statistics panel:
  * Tool counts per type
  * Average confidence scores
  * Tool usage frequency chart
- Chat interface for video Q&A (uses /api/analysis endpoint)
- Export report button

Reports Page (/reports - app/reports/page.js):
- List of generated reports
- Search and filter reports
- Download/share functionality
- PDF viewer for report preview

Required Components (app/components/):
- VideoPlayer.js - Video playback with HLS support
- ToolDetectionOverlay.js - Overlay bounding boxes on video
- ToolTimeline.js - Interactive timeline of tool detections
- ChapterTimeline.js - Video chapters visualization
- EventTimeline.js - Key events timeline
- ReportViewer.js - PDF report viewer
- VideoChat.js - Chat interface for video Q&A
- UploadVideo.js - Video upload component
- NavBar.js - Navigation bar component

TECHNICAL IMPLEMENTATION:

Backend (FastAPI):
```python
# Tool Detection Class - USE PRE-TRAINED MODEL
from ultralytics import YOLO
import os

class SurgicalToolDetector:
    def __init__(self):
        # Use the existing pre-trained model (training already complete)
        model_path = os.path.join("cv_model", "training_scripts", "best.pt")
        if not os.path.exists(model_path):
            raise FileNotFoundError(f"Pre-trained model not found: {model_path}")
        self.model = YOLO(model_path)  # Load existing trained model

    def detect_tools(self, video_path):
        # Use the pre-trained model for inference
        results = self.model.predict(
            video_path,
            conf=0.4,
            iou=0.45,
            imgsz=640,
            save=True
        )
        # Process results: extract tool names, confidence, bboxes, timestamps
        return processed_detections
```

API Endpoints Summary:
- POST /api/video - Upload video file and get video list (use code example B above)
- POST /api/analysis - Twelve Labs analyze API (use code example A above)
- GET /api/analysis/[videoId] - Get video gist (use code example E above)
- POST /api/search - Semantic video search (use code example C above)
- POST /api/timeline - Generate timeline (use code example D above)

NEW ENDPOINTS TO IMPLEMENT:
- GET /api/procedure/[id] - Get procedure data (combine video info + tool detection results + analysis)

Video Processing Flow:
1. User uploads video → Use existing POST /api/video endpoint
2. Backend worker processes video:
   - Chunk video into manageable segments
   - For each chunk:
     * Run YOLO tool detection (using pre-trained best.pt)
     * Draw bounding boxes on frames
     * Generate annotated video
     * Upload to Twelve Labs index
3. Video is indexed in Twelve Labs (Marengo and Pegasus indexes)
4. Frontend can now:
   - Use POST /api/analysis with specific prompts for tool detection results
   - Use POST /api/analysis with specific prompts for report generation
   - Use POST /api/analysis for chat/Q&A (just pass userQuery and videoId)
   - Use POST /api/search for semantic video search
   - Use POST /api/timeline for timeline generation

Frontend (Next.js) - Create from scratch:
- Pages structure (as described in UI Components section above):
  * / (dashboard) - app/page.js
  * /upload - app/upload/page.js
  * /procedure/[id] - app/procedure/[id]/page.js
  * /reports - app/reports/page.js
- Components (create in app/components/):
  * VideoPlayer.js - Video playback with HLS.js
  * ToolDetectionOverlay.js - Overlay tool detection boxes
  * ToolTimeline.js - Tool detection timeline
  * ChapterTimeline.js - Video chapters
  * EventTimeline.js - Key events
  * ReportViewer.js - PDF viewer
  * VideoChat.js - Chat interface
  * UploadVideo.js - Upload component
  * NavBar.js - Navigation
- Use Tailwind CSS for styling
- Medical/clinical theme (blues, whites, greens)
- Responsive design (mobile, tablet, desktop)
- Use Heroicons for icons (@heroicons/react)

Package Dependencies (package.json):
```json
{
  "dependencies": {
    "@aws-sdk/client-s3": "^3.890.0",
    "@aws-sdk/s3-request-presigner": "^3.890.0",
    "@heroicons/react": "^2.2.0",
    "aws-sdk": "^2.1692.0",
    "hls.js": "^0.8.5",
    "next": "15.5.3",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "twelvelabs-js": "^1.0.2"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "15.5.3",
    "tailwindcss": "^4"
  }
}
```

ENVIRONMENT VARIABLES (Create .env.local file - DO NOT include actual API keys):
Required environment variables (user will add their own values to .env.local):
- TWELVELABS_API_KEY=your_api_key_here
- NEXT_PUBLIC_TWELVELABS_MARENGO_INDEX_ID=your_marengo_index_id
- NEXT_PUBLIC_TWELVELABS_PEGASUS_INDEX_ID=your_pegasus_index_id
- AWS_REGION=your_aws_region (for S3 video storage)
- AWS_ACCESS_KEY_ID=your_aws_access_key
- AWS_SECRET_ACCESS_KEY=your_aws_secret_key
- AWS_SOURCE_S3_BUCKET=your_s3_bucket_name

IMPORTANT:
- Generate code that reads from process.env (like the API route examples above)
- Use placeholder values in .env.local.example file
- User will add their actual API keys to .env.local file after code generation
- NEVER hardcode API keys in the generated code

DELIVERABLES:
1. Complete Next.js frontend with all UI components
2. FastAPI backend with surgical tool detection integration (using PRE-TRAINED best.pt model)
3. Twelve Labs API integration (upload, search, chat, chapters) - use exact code patterns above
4. Automated report generation (PDF/JSON) via /api/analysis endpoint
5. Video processing pipeline with real-time progress
6. Error handling and loading states
7. Responsive, professional UI

IMPORTANT REMINDERS:
- DO NOT train a new model - use cv_model/training_scripts/best.pt (already trained)
- Use the exact API endpoint code patterns provided above
- The model training is complete - just integrate it into the application
- Follow the Twelve Labs API integration patterns exactly as shown

SUCCESS CRITERIA:
✅ Upload surgical videos and process successfully
✅ Detect all 7 tool classes with high accuracy (using best.pt model)
✅ Generate comprehensive procedure reports
✅ Enable semantic video search via Twelve Labs
✅ Interactive video playback with tool detection overlays
✅ Chat-based Q&A about video content
✅ Export reports in multiple formats

Start by creating the project structure, then implement video upload/processing using the exact API patterns above, tool detection integration (using the PRE-TRAINED best.pt model - do not train), Twelve Labs integration (use the exact code examples provided), UI components, and finally report generation. Follow the Twelve Labs instruction guide for proper API integration.

REMEMBER:
- The surgical tool detection model is already trained and ready to use. Simply load best.pt and use it for inference - no training needed.
- Use the exact API endpoint code patterns provided above - they are already tested and working.
- All code examples are provided in this prompt - no external file references needed.

